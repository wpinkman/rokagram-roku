APPNAME = rokagram-roku
VERSION = 1.0
DEVPASSWORD=1141
ZIP_EXCLUDE= -x \*.pkg -x storeassets\* -x components\* -x keys\* -x \*/.\* -x Makefile -x app.mk -x .svn\* -x .project -x .DS_Store
include app.mk
